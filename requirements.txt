# FileMorph — dependências
#
# As dependências estão organizadas por fase de implementação.
# Na entrega atual (Fases 1 a 4), os blocos "Interface", "Imagens" e
# "PDF" são os que o aplicativo realmente usa. As demais bibliotecas já
# estão listadas para que os conversores futuros (Fases 5+) não exijam
# reinstalação de ambiente.

# --- Interface (Fase 1-2) ---
PySide6>=6.6.0

# --- Imagens (Fase 3) — obrigatória: sem ela não há conversão ---
Pillow>=10.0.0

# --- PDF (Fase 4) ---
# pypdf junta PDFs; PyMuPDF rasteriza páginas em imagem. Cada uma é
# verificada em separado: sem PyMuPDF, "PDF -> imagens" some da
# interface, mas "imagens -> PDF" e a junção continuam funcionando.
pypdf>=4.0.0
PyMuPDF>=1.23.0

# --- Documentos (Fase 7) ---
python-docx>=1.1.0

# --- Planilhas (futuro) ---
openpyxl>=3.1.0

# --- Empacotamento (Fase 10) ---
pyinstaller>=6.3.0

# --- Desenvolvimento (para rodar a suíte de testes) ---
pytest>=8.0.0

# Observações:
# - Áudio/Vídeo (Fase 6) dependem do FFmpeg, que é um binário externo
#   detectado em tempo de execução por app/utils/ffmpeg_manager.py —
#   não é um pacote pip.
# - Documentos complexos (Fase 7) podem depender de LibreOffice em modo
#   headless, também um binário externo, não um pacote pip.
