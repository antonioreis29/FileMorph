# FileMorph — versões testadas para gerar a versão distribuída
#
# As listas requirements*.txt dizem o mínimo que funciona; este arquivo diz
# exatamente o que foi testado junto (suíte completa, build do PyInstaller,
# instalação e verificação do executável) com Python 3.13 64 bits no
# Windows 11. Use-o para montar o instalador, para que um pacote publicado
# depois não quebre o build de uma versão antiga:
#
#     python -m pip install -r requirements-build.txt -r requirements-dev.txt -c constraints-release.txt
#
# Atualizar uma versão aqui é uma decisão: rode a suíte e o empacotar.ps1
# de novo antes de gravar a mudança.

# Aplicativo
PySide6==6.11.2
PySide6-Essentials==6.11.2
PySide6-Addons==6.11.2
shiboken6==6.11.2
pillow==12.3.0
pypdf==6.18.0
PyMuPDF==1.28.2
python-docx==1.2.0
lxml==6.1.3
typing_extensions==4.16.0
openpyxl==3.1.5
et_xmlfile==2.0.0

# Empacotamento
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
pefile==2024.8.26
pywin32-ctypes==0.2.3
packaging==26.3
setuptools==84.0.0

# Testes
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0
colorama==0.4.6
pygments==2.21.0
